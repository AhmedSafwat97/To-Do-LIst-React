import React from 'react';
import "./loading.css"


const Loading = () => {

    // const Loading = ({ balls, red }) => (
    //     <ReactLoading type={balls} color={red} height={667} width={375} />
    // );
    return (
        <div className='load'>
          <div className="loading"></div>
      </div>
    );

}

export default Loading;
